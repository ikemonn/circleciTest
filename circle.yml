general:
  artifacts:
    - buildCnt.txt
machine:
  node:
    version: 0.12.6
test:
  override:
    - echo "Hello circleCI"
    # - echo 5 > /home/ubuntu/buildCnt.txt
    # - case $CIRCLE_NODE_INDEX in 0) exit 0 ;; 1) exit 1 ;; esac:
    #     parallel: true
    - sleep 120
    # - exit 1
    # - sleep 120
  post:
    # - echo "post Test"
    # - echo project $CIRCLE_PROJECT_REPONAME
    # - echo user $CIRCLE_PROJECT_USERNAME
    # - echo buildNum $CIRCLE_BUILD_NUM
    # - echo retryCnt $KARTE_RETRY_COUNT
    # - printenv
    # - curl https://circleci.com/api/v1/project/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME/$CIRCLE_BUILD_NUM
    - sh ./rebuild.sh
